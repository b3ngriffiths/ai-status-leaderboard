## What does this PR do?

<!-- One sentence summary -->

## Checklist

- [ ] `npx tsc --noEmit` passes locally
- [ ] `npm run validate` passes locally
- [ ] If adding a company: `npm run discover -- <url>` output included and component IDs verified
- [ ] No hardcoded `REPLACE_WITH_REAL_ID` placeholders remaining
